def train_model(X_train, y_train):
    """
    Trains a classification model using the provided training data.

    Parameters:
    X_train : array-like
        Feature data used for training the model.
    y_train : array-like
        Target labels corresponding to the training data.

    Returns:
    model : Trained model object
        The trained classification model.
    """
    pass


def evaluate_model(model, X_test, y_test):
    """
    Evaluates the performance of the trained classification model on the test data.

    Parameters:
    model : Trained model object
        The classification model to evaluate.
    X_test : array-like
        Feature data used for testing the model.
    y_test : array-like
        Target labels corresponding to the test data.

    Returns:
    metrics : dict
        A dictionary containing evaluation metrics such as accuracy, precision, recall, etc.
    """
    pass
